extends layout

block content
    p Not functional yet. Waiting for the database to get set up
    .main.container
        .row
            .col-md-6.col-md-offset-3
                h1.display-4.m-b-2 To contact me

                // register form not function as there is no db yet
                form(method='...' action='...')
                    div.form-group
                        label(for='name') Name:
                        input#name.form-control(type='text', placeholder='First and last' name='name')
                    div.form-group
                        label(for='email') Email:
                        input#email.form-control(type='email', placeholder='xyz@email.com' name='email')
                    div.form-group
                        label(for='message') Message:
                        textarea#message.form-control(rows="4" name='message')
                    button.btn.btn-primary(type='submit') Send